# PRISM-AI CI/CD Pipeline
stages:
  - test

prism-health-check:
  stage: test
  image: python:3.11-slim
  script:
    - echo "PRISM-AI repository verified"
    - echo "Backend and frontend code present"
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
